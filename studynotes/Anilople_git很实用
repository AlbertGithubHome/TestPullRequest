    我通过廖雪峰老师的git教程简单学习了git后，尝试着将自己平时写的
代码（主要是自己实现的一些数据结构和算法）和git结合起来，发觉对于我
这种代码模式不固定，并且会尝试着用不同的方法去实现同一个东西的人来说，
git简直是非常方便。
    例如我最近在写八皇后，先是用了8*8二维数组+模拟+暴搜的方法，
后边又写了8*一维数组的方法（有些遗憾，效率还是很低，不知道为什么）。
要是过去的话，我会建立一个八皇后的文件夹，并把这两个版本的.cpp文件放进去，
然后名称写上简介。但是，有了git，你懂得，在获得了更高效率，更佳的管理的前提下，
依然保持了“信息不减”这个属性，持续了一年的轻微强迫症得以解决了！
